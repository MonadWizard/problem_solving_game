name = "grand-algorithm-verify"
main = "verify.js"
compatibility_date = "2024-01-01"

# No routes/zones configured — deploys to a *.workers.dev URL by default.
# Free tier: 100,000 requests/day, no billing required.
